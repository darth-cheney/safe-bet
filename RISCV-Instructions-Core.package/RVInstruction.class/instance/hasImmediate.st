as yet unclassified
hasImmediate
	"Respond true if this instruction type contains an immediate value.
	Default response is true, so subclasses without immediates should
	override to false explicitly"
	^ true